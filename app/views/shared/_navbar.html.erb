<nav class="navbar navbar-expand-lg bg-primary">
  <ul class="navbar-nav">
    <li class="nav-item active"><%= link_to 'Strona główna', new_user_session_path, class: "text-white nav-link" %></li>
    <li class="nav-item active"><%= link_to 'Wyszukiwania', queries_path, class: "text-white nav-link" %></li>
    <% if current_user && current_user.admin? %>
      <li class="nav-item active"><%= link_to('Panel administratora', admin_index_path, method: :get, class: "text-white nav-link")%></li>
    <% end %>
  </ul>
  <ul class="navbar-nav ml-auto">
    <% if current_user %>
      <li class="nav-item active"><%= link_to('Wyloguj się', destroy_user_session_path, method: :delete, class: "text-white nav-link")%></li>
    <% end %>
  </ul>
</nav>
